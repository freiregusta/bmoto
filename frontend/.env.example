# frontend/.env.example — variáveis de ambiente do frontend
# Copie para .env.local para desenvolvimento local

# URL da API backend (sem barra no final)
VITE_API_URL=https://api.bmoto.com.br

# Senha do dashboard
VITE_DASHBOARD_PASSWORD=senha-forte-aqui
